<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.i4.i4blog.repository.interfaces.user.UserProfileRepository">
    <insert id="save">
        INSERT INTO profile(user_id, profile_title)
        SELECT MAX(u.id) as dd, concat(user_nickname, '님의 블로그 입니다')
        FROM user u
        where id = (SELECT MAX(id) FROM user)
    </insert>
    <update id="saveNickname">
        update user
        set user_nickname = #{nickname}
        where id = #{id}
    </update>
    <update id="saveImg">
        update profile
        set img_original = #{originalImg},
            img_thumbnail =#{thumbNail}
        where user_id = (select id
                         from user
                         where user_id = #{userId})
    </update>
    <update id="changePw">
        update user
        set user_password = #{newPw}
        where id = #{id}
    </update>
    <update id="saveTitleInfo">
        update profile
        set profile_title = #{title} , profile_content=#{info}
        where user_id =#{id}
    </update>
    <update id="resign">
        update user
        set user_state =0
        where user_id =#{userId}
    </update>
    <select id="findByUserId" resultType="com.i4.i4blog.repository.model.user.UserProfile">
SELECT p.id
     , u.id             AS user_id
     , profile_title
     , profile_content
     , img_original
     , img_thumbnail
     , user_nickname
     , user_email
FROM user u
         JOIN profile p ON u.id = p.user_id
where u.id = #{id}
    </select>
    <select id="imgPaths" resultType="com.i4.i4blog.repository.model.user.UserProfile">
        SELECT img_original,img_thumbnail FROM profile
        WHERE user_id = #{id}
    </select>
</mapper>