<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.i4.i4blog.repository.interfaces.board.BoardRepository">

	<insert id="insert">
		INSERT INTO BOARD (user_id, board_title, board_content, board_category)
		 VALUE (1, #{boardTitle}, #{boardContent},#{boardCategory})
	</insert>

	<select id="findByUserId" resultType="com.i4.i4blog.repository.model.board.Board">
		SELECT *
		  FROM board
		 WHERE user_id = #{userId}
	</select>
	
	<update id="deleteById">
		UPDATE board
		SET board_state = 0
		WHERE id=#{id}
	</update>
	
	<select id="findById" resultType="com.i4.i4blog.repository.model.board.Board">
		SELECT *
		  FROM board
		 WHERE id = #{id}
	</select>
	
	<update id="updateCount">
		UPDATE board
		   SET board_count = board_count + 1
		 WHERE id = #{id}
	</update>
	
</mapper>