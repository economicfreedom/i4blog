<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.i4.i4blog.repository.interfaces.gpt.GPTRepository">

    <insert id="save">
        insert into gpt(user_id,request,response)
        value(#{userId},#{request},#{response})
    </insert>
</mapper>